# Production Environment Variables

# Database Configuration
DB_NAME=restaurant_inventory_prod
DB_USER=postgres
DB_PASSWORD=your-super-secure-password-here-change-this

# JWT Configuration (สร้าง secret ใหม่ด้วย openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-32-chars-min
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production-32-chars

# API Configuration
VITE_API_BASE_URL=https://your-domain.com/api/v1

# Production Environment
NODE_ENV=production

# Optional: Email Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Optional: Redis Configuration (for caching)
REDIS_URL=redis://localhost:6379